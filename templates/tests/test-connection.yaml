apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pmsastro.fullname" . }}-test-connection"
  labels:
    {{- include "pmsastro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pmsastro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmsastro.fullname" . }}
  labels:
    {{- include "pmsastro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pmsastro.selectorLabels" . | nindent 4 }}
